{
  "name": "lazy-mans-reminders",
  "description": "Add and complete reminders on the Lazy Man's Reminders lock-screen board. Sign in when asked.",
  "version": "1.1.0",
  "author": {
    "name": "Edmund Lim"
  },
  "mcp": "./mcp.json",
  "variables": {
    "type": "object",
    "properties": {
      "LMR_AGENT_TOKEN": {
        "type": "string",
        "title": "Agent token",
        "description": "Minted on the board under Agent access → Advanced. Format lmr_<uuid>_<secret>. Never commit this value."
      }
    },
    "required": ["LMR_AGENT_TOKEN"]
  }
}
